// Build a function that returns an array of integers from n to 1 where n>0.
const reverseSeq = n => {
    let rezult =[];
    for (let i=n; i>0; i--) {
        rezult.push(i);
    }
    return rezult;
  };
console.log(rezult);